<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== Camera ===== -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />

    <!-- ===== Microphone (passive voice analysis during calls) ===== -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ===== Phone call state (triggers voice analysis) ===== -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- ===== Body sensors ===== -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <!-- ===== System overlay (camera-area hot-zone) ===== -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ===== Foreground service ===== -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Needed to keep the service alive when the screen is off -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- ===== Network ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ===== Storage ===== -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- ===== Notifications ===== -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ===== Alarms (hourly snaps + legacy daily trigger) ===== -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <!-- ===== Boot (restart service after reboot) ===== -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OvulationHealth"
        android:name=".OvulationHealthApp">

        <!-- ===== Main Activity ===== -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OvulationHealth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== Foreground monitoring service (runs all day) ===== -->
        <service
            android:name=".service.ContinuousMonitoringService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone|dataSync">
        </service>

        <!-- ===== Data-sync service (upload to admin server) ===== -->
        <service
            android:name=".service.DataSyncService"
            android:exported="false" />

        <!-- ===== Broadcast Receivers ===== -->

        <!-- Legacy daily alarm (kept for backward compat) -->
        <receiver
            android:name=".receiver.DailyTestReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ovulation.DAILY_TEST" />
            </intent-filter>
        </receiver>

        <!-- Phone call state – triggers passive voice analysis -->
        <receiver
            android:name=".receiver.PhoneCallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Restart monitoring service after device reboot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Notification Channel meta -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>
